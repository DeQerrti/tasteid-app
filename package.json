{
  "name": "tasteid",
  "version": "0.6.5",
  "private": true,
  "description": "TasteID — личный паспорт интересов. Настольное приложение: данные лежат папкой на твоём диске.",
  "type": "module",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "serve": "node scripts/serve.js",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "node --test tests/*.test.js",
    "check": "npm run lint && npm run format:check && npm run check:assets && npm run check:android && npm run check:mobile-links && npm run check:mobile-downloads && npm run check:dupes && npm run check:i18n && npm run check:shared-head && npm run build:mobile && npm test",
    "check:i18n": "node scripts/check-i18n.js",
    "check:shared-head": "node scripts/check-shared-head.js",
    "build": "npm run build:mobile && electron-builder --publish never",
    "build:win": "npm run build:mobile && electron-builder --win --publish never",
    "build:mac": "npm run build:mobile && electron-builder --mac --publish never",
    "build:linux": "npm run build:mobile && electron-builder --linux --publish never",
    "check:assets": "node scripts/check-assets.js",
    "build:mobile": "esbuild mobile/src/main.js --bundle --format=iife --outfile=app/js/mobile.bundle.js --minify --legal-comments=none",
    "mobile:sync": "npm run build:mobile && cap sync android",
    "mobile:apk": "npm run mobile:sync && cd android && ./gradlew assembleDebug",
    "check:android": "node scripts/check-android.js",
    "check:mobile-links": "node scripts/check-mobile-links.js",
    "check:mobile-downloads": "node scripts/check-mobile-downloads.js",
    "check:dupes": "node scripts/check-duplicate-functions.js",
    "test:browser": "node scripts/run-browser-tests.js"
  },
  "dependencies": {
    "electron-updater": "^6.8.9",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/app": "^8.1.1",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/filesystem": "^8.1.3",
    "@capacitor/share": "^8.0.1",
    "@capacitor/status-bar": "^8.0.3",
    "@capawesome-team/capacitor-file-opener": "^8.0.2",
    "electron": "^43.0.0",
    "electron-builder": "^26.0.0",
    "esbuild": "^0.28.2",
    "eslint": "^9.39.5",
    "globals": "^17.11.0",
    "playwright": "1.56.1",
    "prettier": "^3.9.6"
  },
  "build": {
    "appId": "ru.tasteid.app",
    "productName": "TasteID",
    "directories": {
      "output": "dist"
    },
    "files": [
      "app/**/*",
      "core/**/*",
      "electron/**/*",
      "package.json"
    ],
    "asarUnpack": [
      "node_modules/sharp/**",
      "node_modules/@img/**"
    ],
    "publish": {
      "provider": "github",
      "owner": "DeQerrti",
      "repo": "tasteid-app"
    },
    "win": {
      "target": [
        "nsis"
      ],
      "icon": "app/icons/icon-512.png"
    },
    "nsis": {
      "oneClick": true,
      "artifactName": "${productName}-Setup-${version}.${ext}"
    },
    "mac": {
      "target": [
        {
          "target": "dmg",
          "arch": [
            "arm64",
            "x64"
          ]
        }
      ],
      "icon": "app/icons/icon-512.png",
      "category": "public.app-category.lifestyle"
    },
    "linux": {
      "target": [
        "AppImage"
      ],
      "icon": "app/icons/icon-512.png",
      "category": "Utility"
    },
    "artifactName": "${productName}-${version}-${arch}.${ext}"
  }
}
