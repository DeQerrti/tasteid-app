{
  "name": "tasteid",
  "version": "0.1.6",
  "private": true,
  "description": "TasteID — личный паспорт интересов. Настольное приложение: данные лежат папкой на твоём диске.",
  "type": "module",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "serve": "node scripts/serve.js",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "node --test tests/*.test.js",
    "check": "npm run lint && npm run format:check && npm run check:assets && npm test",
    "build": "electron-builder --publish never",
    "build:win": "electron-builder --win --publish never",
    "build:mac": "electron-builder --mac --publish never",
    "build:linux": "electron-builder --linux --publish never",
    "check:assets": "node scripts/check-assets.js"
  },
  "devDependencies": {
    "electron": "^43.0.0",
    "electron-builder": "^26.0.0",
    "eslint": "^9.39.5",
    "globals": "^17.11.0",
    "prettier": "^3.9.6"
  },
  "build": {
    "appId": "ru.tasteid.app",
    "productName": "TasteID",
    "directories": {
      "output": "dist"
    },
    "files": [
      "app/**/*",
      "electron/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        "nsis"
      ],
      "icon": "app/icons/icon-512.png"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "artifactName": "${productName}-Setup-${version}.${ext}"
    },
    "mac": {
      "target": [
        {
          "target": "dmg",
          "arch": [
            "arm64",
            "x64"
          ]
        }
      ],
      "icon": "app/icons/icon-512.png",
      "category": "public.app-category.lifestyle"
    },
    "linux": {
      "target": [
        "AppImage"
      ],
      "icon": "app/icons/icon-512.png",
      "category": "Utility"
    },
    "artifactName": "${productName}-${version}-${arch}.${ext}"
  }
}
