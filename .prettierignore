node_modules
dist
vault
# Фронтенд переехал с сайта как есть и форматируется по своим правилам —
# прогонять по нему prettier значило бы получить огромный бессмысленный
# диff в первом же коммите и потерять сходство с исходником.
app

# Экран приветствия — обычная страница с большими <style> и <script>
# внутри, как и страницы в app/. Prettier раскрыл бы их и дал дифф на
# сотни строк, в котором утонет любая осмысленная правка.
electron/ui
