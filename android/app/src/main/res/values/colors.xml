<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Тот же цвет, что у фона страниц (переменная bg в style.css):
         и пока идёт запуск, и под иконкой приложения экран должен быть
         уже «своим», а не белым.
         Двойной дефис внутри комментария XML запрещён — поэтому имя
         переменной здесь без него, иначе сборка ресурсов падает. -->
    <color name="splash_background">#0a0a0c</color>
    <color name="ic_launcher_background">#0a0a0c</color>
</resources>
